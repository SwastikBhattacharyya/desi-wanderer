server:
  port: 8761
management:
  tracing:
    sampling:
      probability: 1.0
  otlp:
    metrics:
      export:
        url: http://localhost:8918/v1/metrics
  opentelemetry:
    logging:
      export:
        otlp:
          endpoint: http://localhost:8918/v1/logs
    tracing:
      export:
        otlp:
          endpoint: http://localhost:8918/v1/traces
logging:
  pattern:
    correlation:
